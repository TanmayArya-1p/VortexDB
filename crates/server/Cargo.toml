[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
# tokio.workspace = true

api = { path = "../api" }
defs = { path = "../defs" }
index = { path = "../index" }
# storage = { path = "../storage" }
